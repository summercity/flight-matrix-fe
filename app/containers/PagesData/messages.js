/*
 * PagesData Messages
 *
 * This contains all the text for the PagesData container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.PagesData';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PagesData container!',
  },
});
