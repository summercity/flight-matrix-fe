/*
 * NonRecurring Messages
 *
 * This contains all the text for the NonRecurring container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.NonRecurring';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'New',
  },
});
