// import { fromJS } from 'immutable';
// import nonRecurringReducer from '../reducer';

describe('nonRecurringReducer', () => {
  it('returns the initial state', () => {
    expect({}).toEqual({});
  });
});
