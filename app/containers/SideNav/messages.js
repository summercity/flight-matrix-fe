/*
 * SideNav Messages
 *
 * This contains all the text for the SideNav container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SideNav';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SideNav container!',
  },
});
