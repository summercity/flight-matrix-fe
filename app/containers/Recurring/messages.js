/*
 * Recurring Messages
 *
 * This contains all the text for the Recurring container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Recurring';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Recurring container!',
  },
});
