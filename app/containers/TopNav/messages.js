/*
 * TopNav Messages
 *
 * This contains all the text for the TopNav container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.TopNav';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TopNav container!',
  },
});
