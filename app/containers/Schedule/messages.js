/*
 * Schedule Messages
 *
 * This contains all the text for the Schedule container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Schedule';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Schedule container!',
  },
});
