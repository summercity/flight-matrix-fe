/*
 * Authenticated Messages
 *
 * This contains all the text for the Authenticated container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Authenticated';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Authenticated container!',
  },
});
