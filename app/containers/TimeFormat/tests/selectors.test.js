// import { fromJS } from 'immutable';
// import { selectTimeFormatDomain } from '../selectors';

describe('selectTimeFormatDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(false).toEqual(false);
  });
});
