// import { fromJS } from 'immutable';
// import { selectConfirmationDomain } from '../selectors';

describe('selectConfirmationDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
